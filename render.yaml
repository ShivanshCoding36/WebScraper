services:
  - type: web
    name: WebScraper
    env: python
    buildCommand: |
      chmod +x install_chrome.sh
      ./install_chrome.sh
      pip install -r requirements.txt
    startCommand: gunicorn main:app --timeout 300
    plan: free
